# from job_posting.websocket import consumers

# channel_routing = {

#     # Wire up websocket channels to our consumers:
#     'websocket.connect': consumers.ScraperConsumer.connect,
#     'websocket.receive': consumers.ScraperConsumer.receive,
#     'websocket.disconnect': consumers.ScraperConsumer.disconnect,
# }